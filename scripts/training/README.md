# 학습 실행 프리셋

이 폴더에는 과거 실험에 사용한 장비·실험별 `lerobot-train` 실행 프리셋이 있습니다.
각 스크립트는 GPU 번호, 데이터셋, 출력 경로를 고정하며, 일부는 특정 PID 또는 시작
시각까지 포함합니다. 재사용 전 모든 값을 반드시 검토하세요.

일반적인 설정 기반 학습에는 이 스크립트 대신 `scripts/7__train.sh`를 사용합니다.
